#if($word)
  <h1>$word.getWord()</h1>
  #if($word.getDefinitions().size()>0)
    <ul>
    #foreach($definition in $word.getDefinitions())
      <li>$definition.getDefString()</li>
    #end
  </ul>
  #else
    <p>No definitions yet!</p>
  #end
  <a href="/word/$word.getId()/def/new">Add new definition</a><br>
#else
  <h2>Whoops! Looks like we can't find that page :(</h2>
#end
<a href="/">Back to All Words</a>
